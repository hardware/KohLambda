div#admintools
  | Outils d'administration
  ul
    form(action='/cron/to-council', method='POST')
      input(type='hidden', name='secureKey', value="KWotrnskDXDp9MDXNjqTTMHieQDgQjX6yO6TLGAu")
      li
        input(type='Submit', value="Passer au conseil")
    form(action='/cron/to-challenges', method='POST')
      input(type='hidden', name='secureKey', value="KWotrnskDXDp9MDXNjqTTMHieQDgQjX6yO6TLGAu")
      li
        input(type='Submit', value="Passer aux épreuves")

footer

div#debug
  table
    tr
      th GAME_INFO
      th USER_INFO
      th CITY_INFO
    tr
      td
        if !game
          span {nodata}
        else
          ul
            each key, val in game
              li <strong>#{val}</strong> : #{key}
      td
        if !user
          span {error : nodata}
        else
          ul
            each key, val in user
              li <strong>#{val}</strong> : #{key}
      td
        if !city
          span {nodata}
        else
          ul
            each key, val in city
              li <strong>#{val}</strong> : #{key}
